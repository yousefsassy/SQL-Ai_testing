{"id": 1, "split": "dev", "difficulty": "easy", "question": "Count the customers whose country is France. Return exactly one column: the customer count.", "expected_sql": "SELECT COUNT(*) FROM customers WHERE country = 'France';", "order_matters": false}
{"id": 2, "split": "dev", "difficulty": "easy", "question": "List all products in the Electronics category. Return exactly one column: the product name. No particular row order is required.", "expected_sql": "SELECT name FROM products WHERE category = 'Electronics';", "order_matters": false}
{"id": 3, "split": "dev", "difficulty": "easy", "question": "Count the orders placed from January 1, 2025 through December 31, 2025, inclusive. Return exactly one column: the order count.", "expected_sql": "SELECT COUNT(*) FROM orders WHERE order_date >= '2025-01-01' AND order_date < '2026-01-01';", "order_matters": false}
{"id": 4, "split": "dev", "difficulty": "medium", "question": "For each product with at least one order, calculate the total quantity sold across all its orders. Treat each product ID as a separate product. Return exactly two columns, in this order: product name, total quantity sold. No particular row order is required.", "expected_sql": "SELECT p.name, SUM(o.quantity) AS total_quantity FROM products p JOIN orders o ON p.id = o.product_id GROUP BY p.id, p.name;", "order_matters": false}
{"id": 5, "split": "dev", "difficulty": "medium", "question": "Find the single customer who placed the most orders, counting each orders row as one order. Treat each customer ID as a separate customer. If customers tie, choose the customer with the smallest customer ID. Return exactly two columns, in this order: customer name, number of orders.", "expected_sql": "SELECT c.name, COUNT(*) AS order_count FROM customers c JOIN orders o ON c.id = o.customer_id GROUP BY c.id, c.name ORDER BY order_count DESC, c.id ASC LIMIT 1;", "order_matters": true}
{"id": 6, "split": "dev", "difficulty": "medium", "question": "For each product with at least one order, calculate its total revenue across all its orders, using product price multiplied by order quantity. Treat each product ID as a separate product. Return exactly two columns, in this order: product name, total revenue. Do not round the revenue. No particular row order is required.", "expected_sql": "SELECT p.name, SUM(p.price * o.quantity) AS revenue FROM products p JOIN orders o ON p.id = o.product_id GROUP BY p.id, p.name;", "order_matters": false}
{"id": 7, "split": "dev", "difficulty": "hard", "question": "Find the single customer with the highest total spending across all their orders, using product price multiplied by order quantity. Treat each customer ID as a separate customer. If customers tie, choose the customer with the smallest customer ID. Return exactly two columns, in this order: customer name, total spending. Do not round the spending.", "expected_sql": "SELECT c.name, SUM(p.price * o.quantity) AS total_spent FROM customers c JOIN orders o ON c.id = o.customer_id JOIN products p ON o.product_id = p.id GROUP BY c.id, c.name ORDER BY total_spent DESC, c.id ASC LIMIT 1;", "order_matters": true}
{"id": 8, "split": "dev", "difficulty": "hard", "question": "Find the single product category with the highest total revenue across all orders, using product price multiplied by order quantity. If categories tie, choose the category whose name comes first in ascending alphabetical order. Return exactly two columns, in this order: category name, total revenue. Do not round the revenue.", "expected_sql": "SELECT p.category, SUM(p.price * o.quantity) AS revenue FROM products p JOIN orders o ON p.id = o.product_id GROUP BY p.category ORDER BY revenue DESC, p.category ASC LIMIT 1;", "order_matters": true}
{"id": 9, "split": "dev", "difficulty": "hard", "question": "Calculate each customer's total spending across all their orders, using product price multiplied by order quantity. Treat each customer ID as a separate customer. Among customers with at least one order, find those whose total spending is strictly greater than the average of these customer totals. Exclude customers with no orders from the average. Return exactly two columns, in this order: customer name, total spending. Do not round the spending. No particular row order is required.", "expected_sql": "WITH customer_spending AS (SELECT c.id, c.name, SUM(p.price * o.quantity) AS total_spent FROM customers c JOIN orders o ON c.id = o.customer_id JOIN products p ON o.product_id = p.id GROUP BY c.id, c.name) SELECT name, total_spent FROM customer_spending WHERE total_spent > (SELECT AVG(total_spent) FROM customer_spending);", "order_matters": false}
{"id": 10, "split": "dev", "difficulty": "hard", "question": "Find the single product ordered by the largest number of distinct customers, identifying products by product ID and customers by customer ID. Count a customer only once per product, even if they ordered it multiple times. If products tie, choose the product with the smallest product ID. Return exactly two columns, in this order: product name, distinct customer count.", "expected_sql": "SELECT p.name, COUNT(DISTINCT o.customer_id) AS customer_count FROM products p JOIN orders o ON p.id = o.product_id GROUP BY p.id, p.name ORDER BY customer_count DESC, p.id ASC LIMIT 1;", "order_matters": true}
