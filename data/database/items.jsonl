{"id": 1, "split": "dev", "difficulty": "easy", "question": "How many customers are from France?", "expected_sql": "SELECT COUNT(*) FROM customers WHERE country = 'France';", "order_matters": false}
{"id": 2, "split": "dev", "difficulty": "easy", "question": "List the names of all products in the Electronics category.", "expected_sql": "SELECT name FROM products WHERE category = 'Electronics';", "order_matters": false}
{"id": 3, "split": "dev", "difficulty": "easy", "question": "How many orders were placed in 2025?", "expected_sql": "SELECT COUNT(*) FROM orders WHERE order_date >= '2025-01-01' AND order_date < '2026-01-01';", "order_matters": false}
{"id": 4, "split": "dev", "difficulty": "medium", "question": "What is the total quantity sold for each product?", "expected_sql": "SELECT p.name, SUM(o.quantity) AS total_quantity FROM products p JOIN orders o ON p.id = o.product_id GROUP BY p.id, p.name;", "order_matters": false}
{"id": 5, "split": "dev", "difficulty": "medium", "question": "Which customer placed the most orders?", "expected_sql": "SELECT c.name, COUNT(*) AS order_count FROM customers c JOIN orders o ON c.id = o.customer_id GROUP BY c.id, c.name ORDER BY order_count DESC, c.id ASC LIMIT 1;", "order_matters": true}
{"id": 6, "split": "dev", "difficulty": "medium", "question": "What is the total revenue generated by each product?", "expected_sql": "SELECT p.name, SUM(p.price * o.quantity) AS revenue FROM products p JOIN orders o ON p.id = o.product_id GROUP BY p.id, p.name;", "order_matters": false}
{"id": 7, "split": "dev", "difficulty": "hard", "question": "Which customer spent the most money?", "expected_sql": "SELECT c.name, SUM(p.price * o.quantity) AS total_spent FROM customers c JOIN orders o ON c.id = o.customer_id JOIN products p ON o.product_id = p.id GROUP BY c.id, c.name ORDER BY total_spent DESC, c.id ASC LIMIT 1;", "order_matters": true}
{"id": 8, "split": "dev", "difficulty": "hard", "question": "Which product category generated the most revenue?", "expected_sql": "SELECT p.category, SUM(p.price * o.quantity) AS revenue FROM products p JOIN orders o ON p.id = o.product_id GROUP BY p.category ORDER BY revenue DESC, p.category ASC LIMIT 1;", "order_matters": true}
{"id": 9, "split": "dev", "difficulty": "hard", "question": "Which customers spent more than the average amount spent by all customers who placed at least one order?", "expected_sql": "WITH customer_spending AS (SELECT c.id, c.name, SUM(p.price * o.quantity) AS total_spent FROM customers c JOIN orders o ON c.id = o.customer_id JOIN products p ON o.product_id = p.id GROUP BY c.id, c.name) SELECT name, total_spent FROM customer_spending WHERE total_spent > (SELECT AVG(total_spent) FROM customer_spending);", "order_matters": false}
{"id": 10, "split": "dev", "difficulty": "hard", "question": "Which product was ordered by the largest number of different customers?", "expected_sql": "SELECT p.name, COUNT(DISTINCT o.customer_id) AS customer_count FROM products p JOIN orders o ON p.id = o.product_id GROUP BY p.id, p.name ORDER BY customer_count DESC, p.id ASC LIMIT 1;", "order_matters": true}